const addBookButton = document.getElementById('addBook');

let myLibrary = [];

function Book(title, author, pages, isRead) {
    this.title = title;
    this.author = author;
    this.pages = pages;
    this.isRead = isRead;
}

function addBookToLibrary() {
    // executes the command after user fills everything in the input form and clicks the button
}

function displayInputWindow() {
    // displays the input window so user can input a book
}

addBookButton.addEventListener('click', () => {

})